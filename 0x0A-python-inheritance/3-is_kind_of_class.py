#!/usr/bin/python3
def is_kind_of_class(obj, a_class):
    """Function that check if the object is an
        instance of, or if an instance of an inherited
        class"""

    return isinstance(obj, a_class)
